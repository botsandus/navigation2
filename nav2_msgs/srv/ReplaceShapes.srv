# Atomically replace all vector objects. Empty arrays clear all shapes.
# Duplicate nonzero UUIDs or invalid shapes reject the entire snapshot.
# Zero UUIDs are generated by the server, as with AddShapes.
CircleObject[] circles
PolygonObject[] polygons
---
# True when the complete snapshot is accepted.
bool success
