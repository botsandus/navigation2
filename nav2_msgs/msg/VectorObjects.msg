# Aggregate of vector objects for topic-based consumption
# (e.g. by the VectorObjectLayer costmap plugin).
# An empty shape frame_id means the consumer's global frame.

PolygonObject[] polygons
CircleObject[] circles
